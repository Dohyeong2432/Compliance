# Core (required)
networkx>=3.6
PyJWT>=2.13
cryptography>=50.0
fastapi>=0.141
uvicorn>=0.52
pydantic>=2
python-dotenv>=1.0

# /contract-review 엔드포인트: 계약서 파일 업로드(UploadFile, python-multipart 없이는
# FastAPI가 multipart/form-data를 못 읽음) + 검토의견서 .docx 생성(python-docx).
python-multipart>=0.0.9
python-docx>=1.1

# Persistent knowledge backends (Phase 1). Falls back to in-memory stores
# when unset via VECTOR_STORE_BACKEND=memory / GRAPH_STORE_BACKEND=memory.
kuzu>=0.11
chromadb>=1.5

# Real embedder / LLM backends. Only imported when EMBEDDER_BACKEND=voyage
# or the API layer constructs AnthropicLLMClient; not needed for the
# default hash-embedder + scripted-LLM test path.
voyageai>=0.3
anthropic>=0.40

# EMBEDDER_BACKEND=gemini and/or LLM_BACKEND=gemini (POC/무료 AI Studio 키로
# 테스트할 때). Same lazy-import pattern -- only needed when actually selected.
google-genai>=1.0

# Dev/test
pytest>=9
httpx>=0.28
