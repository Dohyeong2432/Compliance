__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/

# Secrets -- never commit. .env.example documents the required keys.
.env
.env.*
!.env.example

# Runtime-generated knowledge backends and logs -- not source documents.
data/chroma/
data/graph.kuzu
data/audit.jsonl
data/sync_state.json
data/law_crawl_state.json
data/embed_cache.json
data/lexical_index.json

# data/raw/ is intentionally tracked: staged source documents (regulations,
# etc.) waiting to be ingested. See data/raw/regulation/README.md.
